<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.atguigu.springcloud.alibaba.dao.AccountDao">

    <resultMap id="BaseResultMap" type="com.atguigu.springcloud.alibaba.domain.Account">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="total" property="total" jdbcType="DECIMAL"/>
        <result column="used" property="used" jdbcType="DECIMAL"/>
        <result column="residue" property="residue" jdbcType="DECIMAL"/>
    </resultMap>

    <update id="decrease">
        UPDATE t_account
        SET
          residue = residue - #{money},used = used + #{money}
        WHERE
          user_id = #{userId};
    </update>

    <update id="updateByUserId">
        update t_account t
        <set>
            <if test="used != null and used != ''">
                t.used = #{used},
            </if>
            <if test="residue != null and residue != ''">
                t.residue = #{residue},
            </if>
        </set>
        where t.user_id = #{userId}
    </update>

    <select id="getByUserId" resultType="com.atguigu.springcloud.alibaba.domain.Account">
        select
            t.id as id,
            t.user_id as userId,
            t.total as total,
            t.used as used,
            t.residue as residue
        from t_account t where t.user_id = #{userId}
    </select>

</mapper>




