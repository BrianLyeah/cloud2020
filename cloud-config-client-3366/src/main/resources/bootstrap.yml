server:
  port: 3366



spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:3344
      label: dev
      name: application
      profile: dev
  #spring rabbitmq相关配置
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

#服务刷新监控点
management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka

